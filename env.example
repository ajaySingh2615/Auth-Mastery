NODE_ENV=development
PORT=5000

DATABASE_URL=

JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

SMTP_HOST=  
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=
```

---

### ✅ Quick Check — Your `src/` should look like this now:
```
src/
├── app.ts                     ✅
└── common/
    └── config/
        └── env.ts             ✅